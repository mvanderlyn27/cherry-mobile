import { ExtendedChapter } from "@/types/app";

export class ReaderService {
  static async getChapterContent(chapter: ExtendedChapter, user_id: string): Promise<string> {
    //get ExtendedChapter info, and load the content
    return "";
  }
}
